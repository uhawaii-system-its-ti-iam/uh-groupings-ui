package edu.hawaii.its.groupings.api;

import edu.hawaii.its.groupings.api.type.Persons;
import edu.hawaii.its.groupings.api.repository.PersonRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.data.domain.Pageable;
import edu.hawaii.its.groupings.api.type.Grouping;
import edu.hawaii.its.groupings.api.type.MyGroupings;
import org.springframework.stereotype.Service;


@Service
@Transactional
public class PageServiceImpl implements PageService{

  //private MyGroupings groupingRepository;
  private PersonRepository presonRepository;

  // private GroupingsService gs;

  @Autowired
  public void PersonServiceImpl(PersonRepository personRepository){
      this.presonRepository = personRepository;
  }


  @Override
  //okay so this is something that gives me what i need
  public Page<Persons>listAllByPage(Pageable pageable){

      return presonRepository.findAll(pageable);
  }


}
